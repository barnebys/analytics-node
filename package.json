{
  "name": "@barnebys/analytics-node",
  "version": "0.1.0",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "test": "yarn run build && mocha",
    "build": "flow-remove-types src/ -d lib/",
    "prepublish": "yarn run build"
  },
  "files": [
    "lib/*"
  ],
  "devDependencies": {
    "flow-bin": "^0.89.0",
    "flow-remove-types": "^1.2.3",
    "mocha": "^5.2.0"
  }
}
